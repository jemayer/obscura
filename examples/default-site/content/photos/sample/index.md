A small collection of sample photographs to demonstrate Obscura's gallery features.

Feel free to replace these with your own images — just drop your photos into the folder and run `npm run build` to generate sidecar files, then `npm run sidecar` to fill in the metadata.
